# Project State

> 给下一位 agent 的交接摘要。任何 agent 接手项目时，读完 `AGENTS.md` 后必须读这里。

## 当前状态

- 项目：Demo_01
- 阶段：first-playable-prototype
- 当前目标：实现浏览器可玩的原创肩后视角生存动作第一版原型。
- 最近一次更新：2026-05-23
- 当前维护者 / 决策人：项目维护者 / Codex
- 远端仓库：<https://github.com/mckf111/simuRE>

## 已完成

- 已安装 vibe-starter-GPT 宪法骨架。
- 已确认第一版技术栈：Web + Three.js + Vite，见 `docs/decisions/0001-game-tech-stack.md`。
- 已建立 npm 脚本：`npm install`、`npm run dev -- --host 127.0.0.1`、`npm run build`。
- 已新增浏览器 3D 游戏入口：`index.html`、`src/main.js`、`src/styles.css`。
- 已实现第一版核心玩法：肩后视角、移动、瞄准、射击、装填、闪避、敌人波次、HUD、补给和死亡重开。
- 已修复开始页点击后不消失的问题；启动流程现在先隐藏覆盖层，再尝试音频和指针锁。
- 已调整准心位置、增加右键拖动视角/方向键视角控制，并中文化 HUD 与战斗提示。
- 已在开始界面加入中文操作说明，开局后随覆盖层隐藏。
- 已将准心改为跟随鼠标，按住鼠标右键拖动会调整视角，并新增 `F` 键近战攻击。
- 已初始化 git 仓库，默认分支为 `main`，并配置远端仓库 `origin` 指向 `mckf111/simuRE`。
- 已完善 README 仓库首页介绍和项目一句话描述。
- 已添加 MIT License，版权行为 `Copyright (c) 2026 mckf111（文虎）`。

## 正在进行

- 等待用户试玩第一版并决定下一阶段方向。
- 后续如果继续扩展，应从新的 `specs/active/<slug>.md` 开始。

## 下一步建议

1. 增加暂停菜单或设置菜单，但避免在游戏画面里堆过多说明文字。
2. 拆分 `src/main.js`，把输入、玩家、敌人、波次和 HUD 分成更小模块。
3. 增加最小自动化测试或浏览器冒烟脚本。
4. 改善敌人导航、碰撞和动画表现。
5. 如果要引入美术资产，先确定授权来源和素材目录规范。

## 待确认

- 是否需要固定使用 Unity / Unreal / Godot 等游戏引擎；如需要，先写新 ADR。
- 是否需要面向键鼠之外的手柄或移动端触控输入。

## 验证状态

- 最快可信验证：`npm run build`
- 最近验证结果：2026-05-23 通过；Vite 提示 Three.js chunk 超过 500 kB，原型阶段接受。
- 完整验证：已启动 `npm run dev -- --host 127.0.0.1`，浏览器确认页面标题、Canvas、中文 HUD、准心跟随鼠标、点击开始后覆盖层隐藏、右键拖动视角事件可用、射击弹药变化和非黑屏渲染。
- 暂未建立的质量门槛：自动化测试、lint、浏览器冒烟脚本。

## 交接注意

- 绝不能引入 RE4/Capcom 的具体角色、剧情、关卡、UI、音效、商标或美术资产。
- 当前目录已初始化 git 仓库，默认分支为 `main`。
- 当前原型没有后端、数据库、环境变量和 secrets。
- `node_modules/` 是本地安装产物，已经由 `.gitignore` 忽略。
